# RP3 Math Notebook (Energy Resource Modelling)

Run `RP3_Math_Energy_Notebook.ipynb` in Jupyter/VSCode.

Data files live in `data/`. Keep the notebook and `data/` folder together.

Change `TECH` near the top of the notebook to select your assigned technology.
